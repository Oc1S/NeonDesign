import React from 'react';
import { Dropdown } from 'neon-ui';
import '../style'; //实际使用时可全局引入样式或使用babel-plugin-import
import '../../button/style'; //实际使用时可全局引入样式或使用babel-plugin-import

export default () => (
  <>
    <Dropdown></Dropdown>
  </>
);
